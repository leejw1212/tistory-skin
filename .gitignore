preview/
gpx/out/
.DS_Store
node_modules/

# 러닝 글 도구
runs/*/
!runs/README.md

# 활동 원본은 공개 저장소에 올리지 않습니다.
# 집 근처 출발점 좌표와 심박이 그대로 들어 있습니다.
# 지도에 나가는 건 images/courses.json 의 단순화된 경로뿐입니다.
gpx/*.tcx
gpx/*.gpx
!gpx/sample.gpx
