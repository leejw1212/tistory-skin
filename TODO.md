# TODO

## 🤖 티스토리 자동 발행 (Playwright)

지금은 `post.md` 를 티스토리 글쓰기 화면에 **붙여넣어** 발행합니다.
마지막 이 한 단계를 없애는 작업입니다.

### 왜 브라우저 자동화인가

티스토리 Open API 는 2023년 12월 ~ 2024년 2월에 단계적으로 종료됐고, **글 작성과 파일 첨부가 가장 먼저 막혔습니다.**
([공지](https://notice.tistory.com/2664) · [개발자 문서](https://tistory.github.io/document-tistory-apis/))
공식 대체 API 가 없고 커넥터도 없어서, 글쓰기 화면을 직접 조작하는 방법밖에 없습니다.

### 만들 것 — `tools/publish-tistory.mjs`

```
node tools/publish-tistory.mjs posts/<폴더> [--visibility private]
```

1. 저장된 카카오 로그인 세션(`storageState`)으로 티스토리 관리 화면 열기
2. 글쓰기 → 에디터를 **마크다운 모드**로 전환
3. `post.md` 본문 입력
4. 카테고리 `러닝코스`, 태그(첫 번째는 지역명), 대표 이미지 지정
5. **비공개로 발행** → 글 주소 회수
6. 회수한 주소를 `images/courses.json` 의 `link` 에 기록

### 설계 메모

- **비공개 발행을 기본값으로 둡니다.** 자동화가 잘못 써도 공개되지 않고, 폰에서 읽어보고 공개 버튼만 누르면 됩니다.
  `--visibility public` 은 명시적으로 줄 때만 동작하게 합니다.
- **로그인 세션**은 최초 1회 사람이 직접 로그인해서 `storageState` 를 뽑아 둡니다.
  카카오는 2단계 인증과 기기 인증이 있어서 자동 로그인은 현실적이지 않습니다.
- **실행 위치는 집 서버를 권장합니다.** 데이터센터 IP 는 카카오 기기 인증에 걸릴 확률이 높습니다.
  GitHub Actions 로 돌린다면 세션은 Secret 에 둡니다.
- 세션은 만료됩니다. 한두 달에 한 번 갱신하는 유지보수가 붙습니다.
- 에디터 DOM 이 바뀌면 깨집니다. **깨졌을 때 조용히 실패하지 않도록** 발행 후 글 주소 회수까지 확인하고,
  실패하면 `post.md` 붙여넣기 방식으로 되돌릴 수 있게 둡니다.

### 선행 확인

- [ ] 티스토리 **대표 이미지(썸네일)** 가 외부 URL(jsDelivr) 사진으로 잡히는지 — 안 되면 대표 이미지 한 장만 따로 업로드
- [ ] 마크다운 모드에서 긴 본문을 한 번에 입력했을 때 서식이 유지되는지

---

## 📥 입력 자동화

- [x] **Dropbox 커넥터 연결** — 완료
- [x] **경로 확인** — `/앱/RunGap/export/` (ns_path `ns:15065913523//export`), 2019-04-30 부터 600개 이상
- [x] **형식 확인** — GPX 가 아니라 **TCX** 였습니다. `tools/tcx-core.mjs` 로 지원 완료
- [x] **원본 TCX 1개로 파서 최종 확인** — 이정욱이 올려준 실제 파일로 검증했고 두 곳을 고쳤습니다
      (기기 자동 랩으로 km 스플릿 / 심박 평균을 랩 시간으로 가중)
- [ ] 1KB 미만 파일이 71% 입니다 (트랙 없는 기록) — 자동으로 거르고 있지만 주기적으로 확인
- [x] **사진을 받는 경로 확인** — **구글 드라이브**입니다. `tools/drive-import.mjs` 로 넣습니다
- [x] **드라이브 사진 폴더 정함** — `러닝기록` (`1jbVrQCDX-cs3sz4NbR1whOHJThy2Fdty`). 2026-09-21 에 만들었고 읽기 확인 완료
- [x] **Dropbox 원본 내려받기** — 허용목록에 `*.dl.dropboxusercontent.com` 이 들어가면서 풀렸습니다.
      `download_link` → `curl` 로 984KB TCX 를 통째로 받아 확인했습니다 (2026-09-21)

## 🗺️ 지도 갱신

- [x] **글 연결 도구** — `tools/link-course.mjs`. 글 번호와 코스 이름을 한 번에 넣습니다
- [x] **업로드 한 번으로** — 글을 먼저 발행하고 연결한 뒤 `courses.json` 을 올리는 순서로 정리
- [x] **코스 이름 자동 생성** — RunGap 파일명 대신 `2026-09-21 경기 4K` 꼴로
- [ ] **장소명 붙이기** — 지금은 날짜·지역·거리뿐입니다. 카카오 로컬 API 키가 있으면 출발지 이름을 넣을 수 있습니다
- [ ] **업로드 자체를 없앨지 결정** — `courses.json` 을 스킨 파일 대신 CDN 에서 읽으면 스킨 편집이 아예 필요 없어집니다.
      대신 외부 호스트에 의존하고 반영이 몇 분 늦습니다

### 사진을 어디서 받을 수 있는가 (확인 완료)

| 경로 | 사진 원본을 받을 수 있나 |
| --- | --- |
| **구글 드라이브** | ✅ 커넥터가 파일을 그대로 돌려줍니다 — 실제 JPEG 으로 EXIF 까지 읽어 확인 |
| **구글 포토** | ❌ 커넥터가 없습니다 (디렉터리에 아예 없음) |
| **Dropbox** | — 사진이 애초에 없습니다. RunGap 은 활동 파일만 올립니다 |
| **프로젝트 채팅에 첨부** | ✅ 항상 됩니다 — 몇 장 안 될 때 제일 빠릅니다 |

## ✍️ 글 품질

- [ ] 출발·도착 지점 장소명 자동 추정 (카카오 로컬 API 키 필요 — 없으면 좌표 보고 물어보기)
- [ ] 같은 코스를 다시 달렸을 때: 새 글 대신 기존 글에 기록 추가할지 정하기
