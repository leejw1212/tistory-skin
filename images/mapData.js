/* =========================================================================
 * 수동으로 관리하는 지도 데이터
 * -------------------------------------------------------------------------
 * 러닝 코스는 이 파일을 건드리지 않습니다.
 *   → tools/gpx-studio.html 에 GPX 를 끌어다 놓거나
 *     `node tools/build-courses.mjs` 를 실행해 images/courses.json 을 만드세요.
 *
 * 여기에는 맛집만 손으로 추가해도 됩니다. (courses.json 의 맛집과 자동으로 합쳐집니다)
 * ========================================================================= */
window.MAP_DATA = {
    /* 지도 데이터를 어디서 읽을지.
       ────────────────────────────────────────────────────────────
       채워두면 스킨에 올린 courses.json 대신 이 주소에서 읽습니다.
       저장소에 푸시하는 것만으로 지도가 갱신되고, 스킨 편집을 열 일이
       없어집니다. (주소를 못 읽으면 스킨에 올린 파일로 알아서 돌아갑니다)

       GitHub 은 5분쯤 캐시하므로 푸시 후 곧 반영됩니다.
       저장소가 공개여야 합니다. 비워두면 예전처럼 스킨 파일을 씁니다. */
    coursesUrl: 'https://raw.githubusercontent.com/leejw1212/tistory-skin/main/images/courses.json',

    /* 구버전 호환용. courses.json 을 쓰면 비워 두세요.
       { filename: "sample.gpx", link: "/12" } 형태로 적으면
       스킨이 GPX 를 직접 내려받아 그립니다. */
    gpxFiles: [],

    restaurants: [
        // {
        //     title: "여의도 수제버거",
        //     url: "/13",                  // 글 주소 (비워두면 제목으로 검색)
        //     lat: 37.5265,
        //     lon: 126.9315,
        //     desc: "러닝 후 단백질 충전"   // 선택
        // }
    ]
};
