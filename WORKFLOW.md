# 🏃 러닝 한 번 → 글 한 편

달리고 나서 글이 올라가고 지도에 코스가 붙기까지, 순서대로 한 장입니다.

```
달린다 ──▶ 재료 모으기 ──▶ 초안 만들기 ──▶ 문장 채우기 ──▶ 발행 ──▶ 연결 ──▶ 푸시
           TCX · 사진        build-post      WRITE 칸        티스토리   link-course   지도 갱신
```

**순서에서 하나만 기억하면 됩니다. 글을 먼저 발행하고, 그다음에 연결합니다.**
지도 데이터에 글 주소가 들어가기 때문입니다. 거꾸로 하면 커밋을 한 번 더 하게 됩니다.

---

## 1. 재료 모으기

러닝 한 번 = 폴더 하나입니다.

```
runs/2026-09-21-여의도/
  activity.tcx      활동 파일 1개   — Dropbox /앱/RunGap/export/ 에서
  IMG_0001.jpg      사진 여러 장    — 순서 상관 없습니다
  IMG_0002.jpg
  notes.md          소감 (선택)     — 메모처럼 편하게
  meta.json         아는 정보 (선택) — 주차 · 급수대 같은 것
```

**활동 파일**은 Dropbox 에 자동으로 쌓입니다. 파일명 앞이 이미 한국 시간이라 어느 러닝인지 바로 보입니다.
1KB 미만 파일은 트랙이 없는 기록이니 크기로 먼저 거르세요.

**사진**은 구글 드라이브의 `러닝기록` 폴더에 올려두면 날짜로 가져옵니다.

```bash
node tools/drive-import.mjs runs/2026-09-21-여의도 --from <받은-폴더> --day 2026-09-21
```

몇 장 안 되면 프로젝트 채팅에 첨부하는 게 제일 빠릅니다. 자세한 건 [`runs/README.md`](runs/README.md).

> 사진을 순서대로 넣을 필요가 없습니다. 촬영 시각과 트랙 시각을 맞춰서
> 출발 전 / 달리는 중 / 완주 후로 알아서 나뉩니다.

---

## 2. 초안 만들기

```bash
node tools/build-post.mjs runs/2026-09-21-여의도
```

한 번에 이걸 다 합니다.

- 거리 · 시간 · 페이스 · 심박 · 케이던스 · 구간 스플릿 계산
- 사진 1600px 리사이즈, **EXIF 위치정보 제거**, 회전 보정
- 사진이 코스 몇 km 지점인지 계산해서 글의 제자리에 배치
- `posts/<날짜-이름>/post.md` 초안 출력
- **`images/courses.json` 갱신** — 지도에 코스가 이때 들어갑니다

> ⚠️ **글을 쓸 때는 `build-courses.mjs` 를 따로 돌리지 마세요.** `build-post` 가 지도까지 갱신합니다.
> 둘 다 돌리면 아이디 규칙이 달라서 같은 러닝이 지도에 두 번 그려집니다.
> (그렇게 되면 `build-post` 가 경고를 띄웁니다)
>
> `build-courses.mjs` 는 **글 없이 지도만** 갱신할 때 씁니다.

> ⚠️ `npm install` 을 한 번은 해두세요. `sharp` 가 없으면 사진 EXIF 가 그대로 남습니다.

---

## 3. 문장 채우기

`post.md` 의 `<!-- WRITE: ... -->` 를 채웁니다. 숫자와 사진 배치는 이미 맞춰져 있으니 문장만 쓰면 됩니다.

채울 곳은 대략 이렇습니다.

| 어디 | 뭘 쓰나 |
| --- | --- |
| 코스 정보 표 | 노면 · 주차 · 접근성 · 급수대 · 화장실 |
| 📝 요약 | **글 전체를 세 줄로.** 스크롤 전에 이것만 읽어도 알게 |
| 주차 & 출발 지점 | 어디 대고 어떻게 가는지 |
| 🏃 코스 따라가기 | **사진과 풍경으로 쓰는 후기.** 숫자는 여기 쓰지 않습니다 |
| ⏱️ 페이스 분석 | 구간 표 + 자동 분석. 그날 몸이 어땠는지만 덧붙이면 됩니다 |
| 누구에게 추천 | 체크박스 몇 개 |
| 다녀온 소감 | 세 줄 정도 솔직하게 |

> 「코스 따라가기」와 「페이스 분석」은 일부러 나눠 뒀습니다.
> 사진을 보며 읽는 부분과 숫자를 보는 부분이 섞이면 둘 다 흐려집니다.
> 페이스 분석의 첫 문단(네거티브 스플릿인지, 심박이 어떻게 움직였는지)은 **자동으로 써집니다.**

`meta.json` 에 미리 적어둔 값이 있으면 그만큼 빈칸이 줄어듭니다.

> ⚠️ **`build-post` 를 다시 돌리면 `post.md` 를 덮어씁니다.** 채워둔 문장이 날아갑니다.
> 사진을 추가하는 등 다시 빌드할 일이 있으면 **문장을 채우기 전에** 하세요.
> 이미 채웠다면 `post.md` 를 먼저 복사해 두세요.
>
> 코스 이름(`--title`)과 글 주소(`link`)는 `courses.json` 에 있어서 다시 빌드해도 남습니다.

---

## 4. 발행하기

티스토리 글쓰기 화면에 `post.md` 를 **마크다운 모드로 붙여넣습니다.**

- 카테고리: `러닝코스`
- 태그: **첫 번째를 지역명으로** 두세요. 근처 맛집 자동 연결이 여기에 의존합니다.
- 대표 이미지: `data.json` 의 `coverCandidates` 참고

발행하면 글 주소가 생깁니다 — `https://jjoyling.tistory.com/213` 의 `213` 이 글 번호입니다.

> 📌 사진은 티스토리에 올리지 않습니다. 저장소의 사진을 CDN 주소로 가리킵니다.
> 그래서 **글을 발행하기 전에 `main` 에 푸시**돼 있어야 사진이 보입니다.

---

## 5. 지도와 글 연결하기

연결은 **두 방향**입니다. 둘 다 자동으로 처리됩니다.

| 방향 | 무엇이 하나 | 어디에 |
| --- | --- | --- |
| **지도 → 글** | `courses.json` 의 `link` | 지도에서 코스를 누르면 글로 갑니다 |
| **글 → 지도** | 본문의 `[course:아이디]` | 글 안에 그 코스 지도가 그려집니다 |

**글 → 지도**는 `build-post` 가 `post.md` 에 `[course:아이디]` 한 줄을 이미 넣어뒀습니다. 손댈 것 없습니다.

**지도 → 글**만 발행 후에 넣어주면 됩니다.

```bash
node tools/link-course.mjs 여의도 213 --title "여의도 한강 4K"
```

- 코스는 아이디 전체를 적지 않아도 됩니다. 이름이나 아이디의 일부만 주면 찾습니다.
- 글 주소는 `213` · `/213` · 전체 URL 아무거나 됩니다.
- `--title` 로 지도에 뜰 코스 이름도 같이 정합니다. 안 정하면 `2026-09-21 경기 4K` 처럼 붙습니다.
- 어떤 코스가 아직 연결 안 됐는지 보려면 인자 없이 실행하세요.

```bash
node tools/link-course.mjs
```

> `link` 가 비어 있어도 지도는 동작합니다. 코스를 누르면 제목으로 블로그 내 검색이 열립니다.

---

## 6. 푸시하기

```bash
git add images/courses.json posts/
git commit -m "🏃 여의도 한강 4K"
git push
```

**5분쯤 뒤 지도에 반영됩니다.** 스킨 편집은 열지 않습니다.

지도가 `images/mapData.js` 의 `coursesUrl` 을 통해 저장소에서 직접 읽기 때문입니다.
그 주소를 못 읽으면 스킨에 올려둔 파일로 알아서 되돌아가므로 지도가 빈 화면이 되지는 않습니다.

---

## 전체를 한 번에

```bash
# 1~2. 재료 넣고 초안
node tools/build-post.mjs runs/2026-09-21-여의도

# 3. post.md 의 WRITE 칸 채우기

# 4. 티스토리에 붙여넣어 발행 → 글 번호 확인 (예: 213)

# 5. 연결
node tools/link-course.mjs 여의도 213 --title "여의도 한강 4K"

# 6. 푸시
git add images/courses.json posts/ && git commit -m "🏃 여의도 한강 4K" && git push
```

---

## 지도만 갱신할 때

글 없이 코스만 지도에 넣고 싶을 때입니다.

```bash
# gpx/ 에 활동 파일을 넣고
node tools/build-courses.mjs
node tools/link-course.mjs <코스> <글번호>   # 연결할 글이 있으면
git add images/courses.json && git commit -m "🗺️ …" && git push
```

여러 개를 한 번에 넣을 수 있습니다. 예시 파일(`sample*`)은 빠집니다.

---

## 막힐 때

| 증상 | 확인할 것 |
| --- | --- |
| 지도가 그대로다 | 푸시했는지, 5분 지났는지. `mapData.js` 의 `coursesUrl` |
| 코스를 눌러도 글로 안 간다 | `link-course.mjs` 로 글 번호를 넣었는지. 카테고리 경로 말고 `/213` 형태여야 합니다 |
| 글에 지도가 안 나온다 | 본문에 `[course:아이디]` 줄이 남아 있는지 |
| 같은 러닝이 지도에 두 번 | `build-courses` 와 `build-post` 를 둘 다 돌린 경우입니다. 한쪽을 `courses.json` 에서 지우세요 |
| 사진이 안 보인다 | `main` 에 푸시했는지. CDN 은 푸시된 파일만 줍니다 |
| 사진에 위치정보가 남는다 | `npm install` 로 `sharp` 를 설치하세요 |
| 사진이 엉뚱한 구간에 붙는다 | 폰 시계. 해외였다면 `--tz` 로 분 단위 오프셋 |

더 자세한 건 [`README.md`](README.md) · [`runs/README.md`](runs/README.md) · [`gpx/README.md`](gpx/README.md).
